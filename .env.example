# Generated checkout-local values belong in `.env`, not this file.
# Run `python3 scripts/bootstrap-checkout.py` in each checkout.

# Managed by bootstrap/setup:
FRICON_WORKSPACE=
DATABASE_URL=

# `bootstrap-checkout.py` aligns `FRICON_WORKSPACE`.
# `setup-dev.py` sets `DATABASE_URL` after the workspace exists.
#
# `DATABASE_URL` should point at the workspace database:
# sqlite:///absolute/path/to/workspace/fricon.sqlite3

# Add any additional local-only variables below.
